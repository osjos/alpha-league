{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "emu": "npx firebase emulators:start --import=./.emulator-data --export-on-exit",
    "rules:deploy": "npx firebase deploy --only firestore:rules",
    "indexes:deploy": "npx firebase deploy --only firestore:indexes",
    "seed": "node scripts/seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^22.13.11",
    "express": "^5.1.0",
    "firebase": "^12.2.1",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0"
  }
}
