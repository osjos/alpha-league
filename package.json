{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "emu": "npx firebase emulators:start --only firestore,auth,ui --project alpha-league-emulator --import=./.emulator-data --export-on-exit",
    "seed": "FIRESTORE_EMULATOR_HOST=127.0.0.1:8085 FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9095 node scripts/seed.js",
    "peek": "FIRESTORE_EMULATOR_HOST=127.0.0.1:8085 node scripts/peek.js",
    "test:rules": "FIRESTORE_EMULATOR_HOST=127.0.0.1:8085 FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9095 node scripts/rule-tests.js",
    "rules:deploy": "npx firebase deploy --only firestore:rules",
    "indexes:deploy": "npx firebase deploy --only firestore:indexes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^22.13.11",
    "express": "^5.1.0",
    "firebase": "^12.2.1",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0"
  }
}
